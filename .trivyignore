# Residual image findings accepted after multi-stage prod image.
# Prefer fixing via base-image / dependency bumps; re-check every release.
#
# No permanent ignores by default — multi-stage + --ignore-unfixed should clear most.
# Add CVE-… lines here only with a date and rationale if a gate blocks without fix.
